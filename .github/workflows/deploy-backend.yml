# Legacy backend deploy — disabled pending migration to Supabase Edge Functions
# This file is kept for reference only.
name: Deploy Backend (Disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Backend now runs on Supabase Edge Functions. This workflow is a placeholder.'
