# Replaced by frontend-deploy.yml (Vercel)
# This file is kept for reference only.
name: Deploy Frontend (Disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Frontend deploy is handled by frontend-deploy.yml via Vercel.'
