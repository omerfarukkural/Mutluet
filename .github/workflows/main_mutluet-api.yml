# Legacy Azure App Service deploy — disabled. No Azure in this project.
# This file is kept for reference only.
name: Azure App Service Deploy (Disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Azure is not used. Backend runs on Supabase.'
